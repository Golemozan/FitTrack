# Arayüz API ile aynı origin'den servis ediliyor (tek Railway servisi),
# bu yüzden göreli yol yeterli — domain değişse bile bozulmaz.
# Parola BURAYA YAZILMAZ — kullanıcı tarayıcıda girer, localStorage'da durur.
VITE_API_URL=/api
